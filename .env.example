# Local development (docker compose up -d db)
DATABASE_URL="postgresql://olympics:olympics@localhost:5433/olympics"
DATABASE_SSL="false"

# Aurora Serverless v2 — direct connection, TLS required.
# Point this at the RDS Proxy endpoint rather than the cluster endpoint.
# DATABASE_URL="postgresql://USER:PASSWORD@proxy-rva4neva.proxy-xxxx.us-east-1.rds.amazonaws.com:5432/olympics?sslmode=require"
# DATABASE_POOL_MAX="1"
